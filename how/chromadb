You can probe our chromadb vectorstore with a search string
by invoking the chromadb tool as shown in the example below:

tool: chromadb
medicare eligibility

Output the above two lines without markdown or comments.
