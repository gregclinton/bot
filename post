# sh post Boss Hal "$(cat apps/wireless/workers/Hal)"
#                              OR
# the string below shows how to post a multi-line message from Boss to Hal:

usage="""
sh post Boss Hal <<EOF
Hello, Hal. I'm your boss.
What are you working on?
EOF
"""

url=$MESSAGES_URL/$2
jq -n --arg frm "$1" --arg body "$3" '{frm:$frm, body:$body}' | curl -X POST "$url" -H "Content-Type: application/json" -d @-
echo
