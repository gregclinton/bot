You can run Linux shell commands like ls, cat, echo, sed, curl, python3, etc 
by invoking the shell tool as shown in the example below:

tool: shell
ls -l

Output the above two lines without markdown or comments.
