# sh apps/setup wireless remote
# sh apps/setup wireless messages

app="$1"
mode="$2"

if [ "$mode" = "messages" ]; then
    rm -rf /tmp/messages
fi

for path in apps/$app/workers/*
do
    name="$(basename "$path")"
    python3 $mode.py post Above "$name" "$(cat "$path")"
done