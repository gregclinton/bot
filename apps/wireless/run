# python3 messages.py post TLG15151515 Hal "$1"

while true # for i in $(seq 1 5)
do
    for worker in Hal Billing Phones Plans
    do
        python3 worker.py groq openai/gpt-oss-120b "$worker"
    done

    for tool in Balance
    do
        python3 tool.py "$tool"
    done

    sleep 0.1
done

# python3 messages.py inbox TLG15151515