account=$1
query=$2

# toy example -- here we would call code to calculate account's balance
# balance=$(sh calc_balance $account)
balance=12.37
when=`date`

echo -n "As of $when, the balance for account $account is \$$balance."