if [ -f Dockerfile ]; then
    rm Dockerfile
    uvicorn bot:app --host 0.0.0.0 --port 4000
else
    systemctl enable ssh
    systemctl start ssh
    git clone https://github.com/gregclinton/bot.git
    cd bot
    sh start
fi
