if [ -f Dockerfile ]; then
    rm Dockerfile
    # ---- edits here donls't require a docker build ----
    rm -rf apps crm
    rm README.md post start
    uvicorn messages:app --host 0.0.0.0 --port 4000 --log-level critical
    # ----------------- end of edits ------------------
else
    rm start
    curl -sL https://github.com/gregclinton/bot/archive/refs/heads/main.zip -o bot.zip
    unzip -q bot.zip
    rm bot.zip
    mv bot-main bot
    cd bot
    sh start
fi
