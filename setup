# sh setup wireless

curl -X DELETE https://"$RUNPOD_POD_ID"-4000.proxy.runpod.net/messages
echo

rm -rf /tmp/workers

for path in apps/"$1"/workers/*
do
    name="$(basename "$path")"
    sh messages/post Above "$name" "$(cat "$path")"
done