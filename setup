# sh setup wireless remote
# sh setup wireless messages

rm -rf /tmp/messages

app="$1"
mode="$2"

for path in apps/$app/workers/*
do
    name="$(basename "$path")"
    python3 $mode.py post Above "$name" "$(cat "$path")"
done